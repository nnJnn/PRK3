#include <stdio.h>
#include <math.h>
#include <string.h>
#include <iostream>

int main() {
	
	char string[] = {'B', 'l', 'a', 'u', ',', ' ', 'G', 'e', 'l', 'b', ',', ' ', 'R', 'o', 't'};
	int **parts;
	int split(char *string, char splitChar, char **parts, int maxParts, int maxLength);
	std::cout << string << std::endl;
	
}
