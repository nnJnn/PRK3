#include <stdio.h>
#include <math.h>
#include <string.h>

int main() {
	
	int *string;
	int **parts;
	int zeichen[25];
	int split(char *string, char splitChar, char **parts, int maxParts, int maxLength);
	
	
	
}
